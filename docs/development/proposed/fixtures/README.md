# Ticket workflow decision fixtures

**INACTIVE proposal.** These fixtures do not install policy, invoke SAGE writes or
constitute installed acceptance. Load the exact ticket and triage candidate files
explicitly in a fresh disposable agent context, then execute the scenarios in
`ticket-decisions.json`. Record observed decisions and candidate hashes outside the
fixture before judging them against the intended outcomes. Semantic labels avoid
coupling acceptance to real portfolio identities.

The namespace scenario must include the higher terminal identity on a later page
and the second project in the same vault; deleting either setup changes what the
trial can establish. The version-history scenario must retain a historical version
higher than the head. The dependency scenario must retain both valid predecessor
and retracted-edge controls. The ranking scenario must retain divergent audience
and reach, duplicate version/edge rows and unknown evidence. These observations
are inputs, not a simulated storage implementation.

For a discriminating prose mutation, reverse only the audience/reach order in a
separate triage copy and run a fresh agent against that copy. Its rank must differ
from the intended CAS order, demonstrating that loaded procedure text actually
influences the decision. Preserve original hashes and confirm unchanged repository
bytes afterward. A missing procedure/setup probe must report the missing evidence,
not infer its result. A text-presence assertion alone is not behavior acceptance.

The deterministic profile test proves ticketing selects its supplement without
making read-only triage govern every write. Removing the ticketing operation or
its procedure source must fail, followed by byte-exact restoration. Validate the
complete inactive profile with the compatible shared resolver in an isolated copy;
never install it in the working checkout to make a test pass.
