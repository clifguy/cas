---
name: smoke-test
description: Route explicitly authorized CAS smoke-test work to the project-owned procedure.
metadata:
  release_status: inactive-source
---

# CAS smoke-test

**INACTIVE staged entry point.** This location is not installed skill discovery.
Resolve the intended project root, then read the compatible shared project-policy
skill and resolve operation `smoke-test`. Require a selected, required binding
with responsibility `smoke-test-procedure` pointing to
`docs/development/operations/smoke-test.md`, resolved from that project root.
Read that complete canonical procedure and its runtime/dependency references.
Missing binding, procedure, authority or capability halts before probes or dispatch
with the missing item and needed configuration named. Do not use this staged file
as implicit activation or fall back to a historical installed procedure.
