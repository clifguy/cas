# Specialized operations source readiness

This is an inactive source candidate. It does not install skills, adopt policy,
claim live operational acceptance or assign a new batch release version.

The canonical smoke procedure covers independent local/cloud SAGE and application
readiness, client/server discrimination, one-write readback and distinct controls.
Deployment retains exact push-to-main revision/attempt gates, bounded waits, dry-run,
explicit override and final refresh, and checks actual dispatch identity/SHA.
Azure review retains the full failure-mode catalog, rename scope and semantic
runtime-dependency triggers. Cohort execution retains all planning, persistence,
pilot, structural-gate, serial-landing and recovery procedures with separate closure.
The source roster records each canonical section; hashes bind the inspected bytes.

Structural checks cover required operation bindings, inert staged discovery, source
and target reference closure, declared capabilities and the independent obligation
roster. Nine deliberate data mutations were detected by assertions, including
removal of capability, file and roster populations; exact source bytes were restored.
The tests assert source wiring only, never agent behavior or live deployment.
Before adding the bindings, the real profile failed the required smoke-binding
assertion. Existing core behavior is a baseline control, not a claimed new fix.

A fresh instruction-following agent exercised disposable service-double fixtures.
The double records chosen actions without deciding policy. The external receipt
retains prompts, fixture observations, action records and limitations; it includes
selected documentation-only smoke, independent readiness, stale-client one-write
readback, blanket-failure controls, exact deployment readiness/races, Azure scope,
overlapping edit windows, both structural gates and serial scoped landing.
These trials provide bounded action-selection evidence. They do not prove live
CI polling, browser behavior, tenant identity, cloud bindings, installation or
all worker/recovery paths. A separate replay uses actual disposable Git objects
for ancestry rather than trusting a fixture's precomputed classification.

The eleven-skill shared core stays independent. The optional package declaration
maps staged entry points to eventual targets and includes complete local runtime
references plus named external capabilities. Package verification and resolver
integration run in the paired shared repository; installation remains separate.

Current CAS authority remains the repository guide/canonical review and live SAGE
steering documents. The inspected invocation and review lineages were linear;
review convention version 18 uses a two-pass unattended cap and retires measurement.
No measurement counter or automatic closure is restored. CPML-finalize is unchanged.

Owner decisions still required: staged policy adoption and coordinated authority
cutover, batch release version, installation/rollback plan, fresh installed discovery
and separately authorized live acceptance. Exact commits, paired package manifests,
validation logs and publication/review receipts belong in the PR evidence.
