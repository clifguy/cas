workflows:
  - workflow_id: test_workflow
    label: Test Workflow
    entry_node: start
    nodes:
      - node_id: start
        label: Start Node
        node_type: decision
        agent_ref: test_agent
    edges:
      - from_node: start
        to_node: start
    state_schema:
      fields:
        - name: doc_id
          type: string
